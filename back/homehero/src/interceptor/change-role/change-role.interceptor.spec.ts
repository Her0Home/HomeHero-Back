import { ChangeRoleInterceptor } from './change-role.interceptor';

describe('ChangeRoleInterceptor', () => {
  it('should be defined', () => {
    expect(new ChangeRoleInterceptor()).toBeDefined();
  });
});
